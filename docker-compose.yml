version: "3.9"

services:
  app:
    build: .
    container_name: my-node-app

    ports:
      - "8000:8000"

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped

    command: npm start
